name: travelkuy
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0 # Versi disesuaikan dengan SDK

flutter:
  uses-material-design: true

  # Folder assets yang sudah disesuaikan
  assets:
    - assets/images/ob1.jpg
    - assets/images/ob2.png
    - assets/images/ob3.png

 